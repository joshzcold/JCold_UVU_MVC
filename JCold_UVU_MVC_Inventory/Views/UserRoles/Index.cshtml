@model IEnumerable<JCold_UVU_MVC_Inventory.Models.UserRoles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserRoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserRoleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserRolesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserRolesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserRolesId })
        </td>
    </tr>
}

</table>
